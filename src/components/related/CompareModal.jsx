const CompareModal = ({features, displayModal}) => {

    return (
      <div className={displayModal ? 'compare modal-show' : 'compare modal-hide'}>
        COMPARE MODAL
      </div>
    );

}

export default CompareModal;